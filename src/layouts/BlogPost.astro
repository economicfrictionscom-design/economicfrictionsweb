---
import BaseLayout from './BaseLayout.astro';
import FormattedDate from '../components/FormattedDate.astro';
import Comments from '../components/Comments.astro';
import Subscribe from '../components/Subscribe.astro';

const { title, description, pubDate, updatedDate } = Astro.props;
---

<BaseLayout title={title} description={description} full={true}>
  <div class="max-w-[100rem] mx-auto px-6 lg:px-12 grid grid-cols-1 lg:grid-cols-[9fr_1fr] gap-8 py-16 relative">
    
    <article class="min-w-0">
      <header class="max-w-3xl mx-auto mb-16 border-b border-slate-100 dark:border-slate-800/50 pb-12">
        <div class="text-amber-600 dark:text-amber-500 font-mono text-[10px] uppercase tracking-[0.3em] mb-6">
          <FormattedDate date={pubDate} />
        </div>
        <h1 class="text-4xl md:text-6xl font-serif font-bold text-slate-900 dark:text-white leading-tight mb-8">
          {title}
        </h1>
        <div class="h-1 w-12 bg-amber-500"></div>
      </header>

      <div class="prose prose-slate dark:prose-invert font-serif max-w-3xl mx-auto prose-lg leading-[1.85] text-slate-800 dark:text-slate-200">
        <slot />
      </div>

      <div id="static-subscribe-section" class="max-w-3xl mx-auto mt-32 border-t border-slate-200 dark:border-slate-800 pt-20">
        <div class="bg-slate-100 dark:bg-slate-800/30 p-10 rounded-[2rem] text-center">
            <Subscribe />
        </div>
        <div class="mt-20">
          <Comments />
        </div>
      </div>
    </article>

    <aside class="hidden lg:block">
      <div id="sidebar-subscribe-container" class="sticky top-32 transition-all duration-700 ease-in-out z-40">
         <div class="bg-white dark:bg-slate-900/40 backdrop-blur-md border border-slate-200 dark:border-slate-800 p-5 rounded-2xl shadow-xl">
            <h3 class="font-serif font-bold text-base text-slate-900 dark:text-white mb-1 tracking-tight">Journal</h3>
            <p class="text-[11px] text-slate-500 dark:text-slate-400 mb-4 leading-snug">
              Analysis of markets & frictions.
            </p>
            
            <form action="https://buttondown.email/api/emails/embed-subscribe/devansh" method="post" target="popupwindow" class="flex flex-col gap-2.5">
                <input 
                  type="email" 
                  name="email" 
                  placeholder="Email" 
                  required 
                  class="w-full px-3 py-2 text-xs rounded-lg border border-slate-200 dark:border-slate-700 bg-slate-50/50 dark:bg-slate-800/50 text-slate-900 dark:text-white outline-none focus:ring-1 focus:ring-amber-500" 
                />
                <input type="hidden" value="1" name="embed" />
                <button type="submit" class="w-full py-2 bg-slate-900 dark:bg-amber-500 text-white dark:text-slate-950 text-[10px] font-black uppercase tracking-widest rounded-lg hover:opacity-90 transition-all">
                  Subscribe
                </button>
            </form>
         </div>
      </div>
    </aside>
  </div>

  <script is:inline>
    const sidebar = document.getElementById('sidebar-subscribe-container');
    const staticSection = document.getElementById('static-subscribe-section');

    window.addEventListener('scroll', () => {
      if (!sidebar || !staticSection) return;
      const staticRect = staticSection.getBoundingClientRect();
      
      if (staticRect.top < window.innerHeight - 50) {
        sidebar.style.opacity = '0';
        sidebar.style.pointerEvents = 'none';
        sidebar.style.transform = 'translateY(10px) scale(0.95)';
      } else {
        sidebar.style.opacity = '1';
        sidebar.style.pointerEvents = 'auto';
        sidebar.style.transform = 'translateY(0) scale(1)';
      }
    });
  </script>
</BaseLayout>